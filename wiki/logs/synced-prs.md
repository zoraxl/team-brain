# Synced PRs

Canonical ledger of merged PRs already ingested by `/wiki-sync`. This file makes PR-mode sync idempotent.

Use one line per synced PR:

```txt
<owner/repo>#<number> synced YYYY-MM-DD <PR URL>
```

